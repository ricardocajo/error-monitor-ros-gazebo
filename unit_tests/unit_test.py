from __future__ import annotations

tests = [("always turtlebot3_burger.position.x >= -1", "rosbag1.bag")]
test_time = 60
ros_workspace_tests_dir = "/home/rcordeiro/ros_workspace/src/test_pkg/src"
# loop tests list
# create file
# compile created file
# open console and run the outputed test file
# open console and run the corresponding bagfile
# open console and run the simulation/robot
# save the output of the test in a var
